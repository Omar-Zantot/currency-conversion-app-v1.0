import { FilterFavCurrencyPipe } from './filter-fav-currency.pipe';

describe('FilterFavCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFavCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
