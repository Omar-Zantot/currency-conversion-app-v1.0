import { GetFavCurrencyPipe } from './get-fav-currency.pipe';

describe('GetFavCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFavCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
